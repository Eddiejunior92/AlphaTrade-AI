# Phase B Paper Trading — Baseline Snapshot

**Captured at:** `2026-05-08 22:54:51 UTC`
**Mode:** read-only diagnostic · no code or data modified
**Source:** production Postgres via `DATABASE_URL`

---

## ⚠️ Important context for this baseline

Several Phase B tables (`dynamic_gate_state`, `symbol_expectancy`,
`calibration_history`, `pending_suggestions`) **do not yet exist in the
database** at the moment of capture. They are created by `db.ensureSchema()`
which runs on backend boot. The currently running workflow is the **Vite
frontend only** — the Node backend (`backend/server.js`) has not been
restarted since the Phase B commit landed, so the migrations have not yet
executed.

This is itself useful baseline information: it confirms the "before" state is
genuinely pre-Phase-B from a DB perspective. The next backend boot will:

1. Create `symbol_expectancy`, `calibration_history`, and any other Phase B
   tables.
2. Initialise the `dynamic_gate_state` boot row.
3. Write the `POSITION_CAP_INCREASED` migration audit row (one-shot, already
   landed in a prior boot — won't fire twice).

---

## Query 1 · `SELECT NOW() AS captured_at`

| captured_at |
|---|
| `2026-05-08 22:54:51.133059+00` |

## Query 2 · Portfolio state

| cash_balance | starting_balance | day_start_equity | agent_running | circuit_breaker | emergency_pause | trading_mode |
|---|---|---|---|---|---|---|
| `22291.07` | `25000.00` | `24085.60` | `true` | `false` | `false` | `paper` |

**Read:** Account is down `$2,708.93` cash vs the original `$25,000` starting
balance. Equity-to-starting drift is `-3.66%` measured against
`day_start_equity`. Agent flag is `true`, no breaker, no pause, paper mode —
exactly the state Phase B expects to start from.

## Query 3 · `SELECT * FROM dynamic_gate_state ORDER BY id DESC LIMIT 1`

```
ERROR: relation "dynamic_gate_state" does not exist
```

**Interpretation:** Phase A.5 schema migration has not yet been applied to
this DB instance. Will be created on next backend boot.

## Query 4 · `SELECT COUNT(*) AS expectancy_rows FROM symbol_expectancy`

```
ERROR: relation "symbol_expectancy" does not exist
```

**Interpretation:** Phase B table — will be created on next backend boot.
Empty by design; this is the desired "before" state.

## Query 5 · `SELECT COUNT(*) AS calibration_rows FROM calibration_history`

```
ERROR: relation "calibration_history" does not exist
```

**Interpretation:** Phase B table — will be created on next backend boot.
Empty by design; this is the desired "before" state.

## Query 6 · `SELECT COUNT(*) AS pending_suggestions FROM pending_suggestions WHERE status='pending'`

```
ERROR: relation "pending_suggestions" does not exist
```

**Interpretation:** Phase A.5 table — will be created on next backend boot.
No suggestions pending; this is the desired "before" state.

## Query 7 · Trade history (last 30 days)

| trades_last_30d | wins | avg_pnl | total_pnl |
|---|---|---|---|
| `5` | `1` | `-54.47` | `-272.35` |

**Read:** 5 closed trades in the last 30 days. **1 win, 4 losses.** Win rate
= 20%. Average P&L = `-$54.47`. Total realised = `-$272.35`. This is the
pre-Phase-B baseline — small sample, but the direction matches the goal of
Phase B (per-symbol auto-suspend would have caught a chronic loser; the
calibration audit would have flagged confidence drift; the regime layer would
have raised the gate during whatever conditions produced the 4 losses).

## Query 8 · Audit log size

| audit_rows | latest_audit_id |
|---|---|
| `6,363` | `6,602` |

**Read:** 6,363 rows present, monotonically-increasing id at 6,602 (gap of
239 indicates some deleted/skipped IDs over time, which is normal for the
Postgres sequence). The hash chain is intact at this point. All Phase B
audit-row event types (`SIZING_BOUND_VIOLATED`, `SYMBOL_AUTO_SUSPENDED`,
`SYMBOL_REINSTATED`, `REINSTATE_UNAUTHORIZED`) start from 0 occurrences as of
this baseline.

## Query 9 · `SELECT COUNT(*) AS total_symbols_tracked FROM symbol_expectancy`

```
ERROR: relation "symbol_expectancy" does not exist
```

**Interpretation:** Same as Query 4 — table not yet created.

## Query 10 · `SELECT COUNT(*) AS suspended_symbols FROM symbol_expectancy WHERE suspended = TRUE`

```
ERROR: relation "symbol_expectancy" does not exist
```

**Interpretation:** Same as Query 4 — table not yet created. Effective
suspension count is `0`.

---

## Baseline summary (the "before" picture)

| Metric | Value |
|---|---|
| Capture time | `2026-05-08 22:54:51 UTC` |
| Equity (day_start) | `$24,085.60` |
| Cash balance | `$22,291.07` |
| Starting balance | `$25,000.00` |
| Drift vs starting | `-3.66%` |
| Trading mode | `paper` |
| Agent running | `true` |
| Circuit breaker / emergency pause | both `false` |
| Trades in last 30d | `5` |
| Win rate (30d) | `20%` (1 of 5) |
| Avg P&L per trade (30d) | `-$54.47` |
| Total P&L (30d) | `-$272.35` |
| Audit rows | `6,363` (latest id `6,602`) |
| Phase A.5 tables present | `dynamic_gate_state`: ❌ · `pending_suggestions`: ❌ |
| Phase B tables present | `symbol_expectancy`: ❌ · `calibration_history`: ❌ |
| Symbols auto-suspended | `0` (table not yet created) |
| Pending config suggestions | `0` (table not yet created) |

## Next steps before observation window starts

1. **Restart the Node backend** so `db.ensureSchema()` creates the Phase A.5
   and Phase B tables. The next `POSITION_CAP_INCREASED` migration row check
   will be a no-op (one-shot already fired in prior boot per `replit.md`).
2. **Re-capture queries 3, 4, 5, 6, 9, 10** post-boot to confirm the empty
   "before" state is in place. (Optional — they'll all be `0` rows by
   construction, since the tables are brand new.)
3. **Begin the 14-day observation window** at the timestamp of the first
   `runCycle` log entry post-boot. That timestamp is the official start of
   the kill-criteria window committed in `OPERATOR_KILL_CRITERIA.md`.

This baseline is locked in. Do not amend it after the agent starts trading;
treat any later "before" capture as a separate document.
